queue_rules:
  - name: default
    merge_conditions: []

pull_request_rules:
  - name: merge when gate green + label
    conditions:
      - check-success=gate
      - label=mergify-go
    actions:
      merge:
        method: squash

queue_rules:
  - name: default
    merge_conditions: []

pull_request_rules:
  - name: merge when ci green
    conditions:
      - check-success=ci
      - "-draft"
    actions:
      merge:
        method: squash

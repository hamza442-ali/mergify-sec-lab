queue_rules:
  - name: default
    conditions: []
pull_request_rules:
  - name: live negative scoped gate
    conditions:
      - check-success=@github-actions/gate
      - "label=mergify-go"
    actions:
      merge:
        method: squash

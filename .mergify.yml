queue_rules:
  - name: default
    conditions: []
pull_request_rules:
  - name: live poc merge when gate green
    conditions:
      - check-success=gate
      - "label=mergify-go"
    actions:
      merge:
        method: squash
